import std.buffer as buf
import std.binary as bin
import std.encoding as enc

let b = buf.new(4)
bin.write_u32_le(b, 0, 305419896)
print(enc.hex_encode(buf.to_bytes(b)))

let b2 = buf.new(4)
bin.write_u32_be(b2, 0, 305419896)
print(enc.hex_encode(buf.to_bytes(b2)))
