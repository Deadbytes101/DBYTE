fn add(a: int, b: int) -> int:
    return a + b

fn mul(a: int, b: int) -> int:
    return a * b

# Nested call in discarded position
add(mul(2, 3), add(4, 5))

# Result should be on i64_stack, then discarded.
# If stack is polluted, next ops fail.
print(add(10, 20))
