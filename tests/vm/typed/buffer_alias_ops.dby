import std.buffer as bytes

let target: buffer = bytes.from_bytes(b"\x00\xDE\xAD\xBE\xEF\x00")

print(bytes.find(target, b"\xDE\xAD\xBE\xEF"))
bytes.replace(target, 1, b"\x90\x90\x90\x90")
print(bytes.to_bytes(target))
